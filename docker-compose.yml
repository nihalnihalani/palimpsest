services:
  redis:
    image: redis/redis-stack:latest
    container_name: wiki-redis
    ports:
      - "6379:6379"
      - "8001:8001"  # RedisInsight UI for demo
    volumes:
      - redis-data:/data
volumes:
  redis-data:
